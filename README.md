## Project Overview
This API microservice was built using Node.js and Express. The app accepts user input of a date in either miliseconds or standard date format and convert it to UTC and Unix timestamp format.

To view app, click <a href="https://glitch.com/~immense-share">HERE</a>.
